Objectif : appliquer un fond blanc aux cartes polaroïd pour respecter l'identité visuelle du projet.

Étapes :
1. Modifier `src/shared/ui/components/PolaroidCard.tsx` : remplacer la classe de fond actuelle (`bg-background` ou équivalent) par `bg-white`.
2. Vérifier que les ombres, bordures et marges restent cohérentes avec le style néo-brutaliste.
3. Contrôler visuellement sur la page Photos et la page d'accueil (carte du concours en cours) qu'aucun contraste ou lisibilité n'est dégradé.

Livrables :
- `src/shared/ui/components/PolaroidCard.tsx` mis à jour.
- Aperçu validé sur les écrans concernés.